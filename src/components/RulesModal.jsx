import { motion } from 'framer-motion';

export default function RulesModal({ onClose }) {
  return (
    <motion.div
      className="overlay"
      onClick={onClose}
      initial={{ opacity: 0 }}
      animate={{ opacity: 1 }}
      exit={{ opacity: 0 }}
    >
      <motion.div
        className="modal"
        onClick={(e) => e.stopPropagation()}
        initial={{ opacity: 0, scale: 0.92, y: 16 }}
        animate={{ opacity: 1, scale: 1, y: 0 }}
        exit={{ opacity: 0, scale: 0.92, y: 16 }}
        transition={{ type: 'spring', stiffness: 320, damping: 28 }}
      >
        <div className="row-between">
          <h3>Правила игры</h3>
          <button className="secondary-btn" onClick={onClose}>
            Закрыть
          </button>
        </div>
        <ul style={{ paddingLeft: 18, lineHeight: 1.6 }}>
          <li>Выберите тему (красная — 12 уровней, зелёная — 9) и ставку в бонусных баллах.</li>
          <li>Ставка списывается сразу после нажатия «Начать».</li>
          <li>Коэффициент растёт, пока летит шар. После первого уровня доступна кнопка «Забрать».</li>
          <li>Если нажать «Забрать» до краха — выигрыш = ставка × текущий коэффициент. Шар при этом
            продолжает лететь дальше — если он улетит выше, экран результата покажет, сколько можно было
            выиграть.</li>
          <li>Если не успеть нажать «Забрать» — ставка сгорает в момент краха.</li>
          <li>
            Бустер (×2/×3/×4) ждёт на случайном уровне — если долететь до него раньше cashout,
            коэффициент резко увеличивается.
          </li>
          <li>За каждый пройденный уровень и за активацию бустера начисляются игровые очки — отдельно от бонусных баллов.</li>
          <li>
            После каждого раунда — независимо от исхода — вы получаете случайный фрагмент пазла (4 вида).
            Фрагменты копятся навсегда (см. значок коллекции на экране ставки): как только наберётся
            хотя бы по одному каждого вида — засчитывается полный комплект и начисляется бонус очков;
            дубликаты не пропадают и считаются в следующий комплект.
          </li>
        </ul>
      </motion.div>
    </motion.div>
  );
}
